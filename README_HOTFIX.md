# AngelBird RMA Warranty verifier hotfix

Fixes the PowerShell parser error caused by JavaScript-style escaping in the verifier.

Replace the root `VERIFY-WEB-RMA-WARRANTY-ISSUES.ps1` with this corrected file and run it again.
