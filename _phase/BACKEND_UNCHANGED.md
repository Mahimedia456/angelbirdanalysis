# Backend preservation note

Phase 02 intentionally makes no backend changes.

The supplied backend already supports login, refresh, current-user/profile lookup, and logout through `/api/auth/*`. Existing web frontend/backend/database/Google Sheet/CSV functionality remains the baseline and is not included in this additive mobile ZIP.
